name: example
description: "An example Flutter project using the dartblock_code package."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.2
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^12.0.0
  flutter_riverpod: ^3.4.2
  google_fonts: ^8.2.1
  json_annotation: ^4.12.0
  dartblock_code:
    path: ../

  material_ui: any
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  json_serializable: ^6.14.1

flutter:
  uses-material-design: true
  assets:
    - assets/dartblockEvaluatorSamples/
    - assets/dartblockSamples/
